#ifndef BASEMANAGER_H
#define BASEMANAGER_H

#include "AbstractFactory/drawer.h"
#include "objects/camera.h"

class BaseManager
{
public:
    BaseManager() = default;
    ~BaseManager() = default;
};

#endif // BASEMANAGER_H
